import themeColors from "./themeColors";

const theme = {
  // fontFamily: "'Segoe UI', sans-serif",
  fontFamily: "'Montserrat', sans-serif",
  headings: {
    fontFamily: "'Montserrat', sans-serif",
  },
  colors: {
    primary: themeColors.color1,
    secondary: themeColors.color1,
  },
  colorScheme: "dark",
  primaryColor: "primary",
  primaryShade: {
    light: 5,
    dark: 5,
  },
};

export default theme;
